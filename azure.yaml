# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aca-tf-java-demo

metadata:
  template: aca-tf-java-demo

infra:
  provider: terraform
  path: ./infra

services:
  backend:
    project: ./src/backend
    language: java
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
